{
  "name": "statistics-orphan-finder-v2",
  "version": "2.0.0-alpha",
  "description": "Statistics Orphan Finder V2 - Lit + TypeScript rewrite",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "tsc && vite build",
    "build:prod": "tsc && vite build --mode production"
  },
  "dependencies": {
    "lit": "^3.1.2"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3",
    "vite": "^5.0.12"
  }
}
